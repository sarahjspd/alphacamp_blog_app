<h1> My <a href="#"><span class="badge">B</span><span class="badge">L</span><span class="badge">O</span><span class="badge">G</span></a> Diaries ! </h1>


<div class="postcountd">You Got <code>
<%= @postcount = Post.count %>
 Posts</code> and Counting...</div>
</div></div></div>

<table class="table table-hover">

	<thead>
		<th>Title</th>
		<th>Author</th>
		<th>Created At</th>

<!-- 		<th>View</th> -->
	</thead>

	<tbody>

	<% @post.each do |post| %>
<!-- why if I don't put the above line it wouldnt work? -->
			<tr>
				<td>
					<%= post.title %>
				</td>
				<td>
					<%= post.author %>
				</td>
				<td>
					<%= post.created_at %>
				</td>
				<td>
					<div class="buttons"><%= link_to "View", post_path(id: post.id) %></div>
				</td>
				<td>
					<div class="buttons"><%= link_to "Delete", post_path(id: post.id), method: :delete %></div>
				</td>
			</tr>
	<% end %>
	</tbody>
</table>
<p></p>
<div class="buttons"><%= link_to "Create New Post", new_post_path %></div>